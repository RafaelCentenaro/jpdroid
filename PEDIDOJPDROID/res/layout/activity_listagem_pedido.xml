<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@android:color/black"
    tools:context=".ListagemPedidoActivity" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white" >

        <EditText
            android:id="@+id/etPesquisaPedido"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ems="10"
            android:inputType="text|textCapWords" >
        </EditText>

        <ImageButton
            android:id="@+id/btLimparPesquisaPedido"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:background="@android:color/white"
            android:onClick="onClickLimparPesquisa"
            android:src="@drawable/delete_icon" />

        <ImageButton
            android:id="@+id/btPesquisaPedido"
            android:layout_width="64dp"
            android:layout_height="fill_parent"
            android:layout_weight="0.42"
            android:background="@android:color/white"
            android:onClick="onClickPesquisaPedido"
            android:src="@android:drawable/ic_menu_search" />
    </LinearLayout>

    <ListView
        android:id="@+id/lvListagemPedido"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:clickable="true"
        android:drawSelectorOnTop="true"
        android:longClickable="true" >
    </ListView>

</LinearLayout>