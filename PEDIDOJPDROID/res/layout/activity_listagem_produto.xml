<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@android:color/black"
    android:orientation="vertical"
    tools:context=".ListagemProdutoActivity" >

      <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" 
        android:background="@android:color/white">

        <EditText
            android:id="@+id/etPesquisaProduto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ems="10"
            android:inputType="text|textCapWords" >
        </EditText>

        <ImageButton
            android:id="@+id/btLimparPesquisaProduto"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:background="@android:color/white"
            android:onClick="onbtLimparPesquisaProduto"
            android:src="@drawable/delete_icon" />

        <ImageButton
            android:id="@+id/btPesquisaProduto"
            android:layout_width="64dp"
            android:layout_height="fill_parent"
            android:layout_weight="0.42"
            android:background="@android:color/white"
            android:onClick="onbtPesquisaProduto"
            android:src="@android:drawable/ic_menu_search" />
    </LinearLayout>

    <ListView
        android:id="@+id/lvListagemProduto"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:clickable="true"
        android:drawSelectorOnTop="true"
        android:longClickable="true" >
    </ListView>

</LinearLayout>